- name: Install nginx
  hosts: frontend
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.package:
        name: nginx
        state: installed

    - name: Start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes 